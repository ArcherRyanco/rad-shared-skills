
# Next.js Scaffolding Skill

This skill provides scaffolding for new Next.js 14+ projects with App Router, including common patterns and integrations.

## Features

-   Create new Next.js 14+ projects with App Router.
-   Default stack: TypeScript, Tailwind CSS, ESLint.
-   Common patterns: layouts, loading states, error boundaries.
-   API routes and server actions templates.
-   Common integrations: Prisma, Auth.js, Stripe snippets.

## Usage

### Create a New Next.js Project

To create a new Next.js project with the default stack (TypeScript, Tailwind CSS, ESLint) and App Router, use the following command:

```bash
npx create-next-app@latest {project-name} --typescript --tailwind --eslint --app
```

**Prompt Example:**

"Create a new Next.js project named `my-dashboard` with TypeScript, Tailwind CSS, ESLint, and App Router."

**Agent Action:**

```bash
npx create-next-app@latest my-dashboard --typescript --tailwind --eslint --app
```

### Common Patterns

#### Layouts (app/layout.tsx)

```tsx
// app/layout.tsx
import './globals.css';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata = {
  title: 'Create Next App',
  description: 'Generated by create next app',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <header className="bg-gray-800 text-white p-4">
          <nav className="container mx-auto">
            <a href="/" className="text-xl font-bold">My App</a>
          </nav>
        </header>
        <main className="container mx-auto p-4">
          {children}
        </main>
        <footer className="bg-gray-200 p-4 text-center">
          © {new Date().getFullYear()} My App
        </footer>
      </body>
    </html>
  );
}
```

**Prompt Example:**

"Generate a basic `app/layout.tsx` for my Next.js project that includes a header, main content area, and footer."

#### Loading States (app/dashboard/loading.tsx)

```tsx
// app/dashboard/loading.tsx
export default function Loading() {
  return (
    <div className="flex justify-center items-center h-screen">
      <div className="animate-spin rounded-full h-32 w-32 border-b-2 border-gray-900"></div>
      <p className="ml-4 text-lg">Loading dashboard...</p>
    </div>
  );
}
```

**Prompt Example:**

"Create a `loading.tsx` file for the `/dashboard` route that shows a spinner and 'Loading dashboard...' text."

#### Error Boundaries (app/error.tsx)

```tsx
// app/error.tsx
'use client'; // Error components must be Client Components

import { useEffect } from 'react';

export default function Error({
  error,
  reset,
}: {
  error: Error & { digest?: string };
  reset: () => void;
}) {
  useEffect(() => {
    // Log the error to an error reporting service
    console.error(error);
  }, [error]);

  return (
    <div className="flex flex-col justify-center items-center h-screen bg-red-100 text-red-800">
      <h2 className="text-2xl font-bold mb-4">Something went wrong!</h2>
      <p className="mb-4">{error.message}</p>
      <button
        className="px-4 py-2 bg-red-600 text-white rounded hover:bg-red-700"
        onClick={
          // Attempt to recover by trying to re-render the segment
          () => reset()
        }
      >
        Try again
      </button>
    </div>
  );
}
```

**Prompt Example:**

"Add an `error.tsx` file to handle errors globally in the Next.js App Router, with a 'Try again' button."

### API Routes (app/api/hello/route.ts)

```tsx
// app/api/hello/route.ts
import { NextResponse } from 'next/server';

export async function GET(request: Request) {
  return NextResponse.json({ message: 'Hello from Next.js API!' });
}

export async function POST(request: Request) {
  const data = await request.json();
  return NextResponse.json({ message: 'Received POST request', data });
}
```

**Prompt Example:**

"Create a new API route at `/api/hello` that responds to GET requests with a 'Hello' message and to POST requests with the received data."

### Server Actions (app/actions.ts or inside component)

```tsx
// app/actions.ts
'use server';

export async function createUser(formData: FormData) {
  const name = formData.get('name');
  const email = formData.get('email');
  // ... perform database operations or other server-side logic
  console.log('Server Action: Creating user', { name, email });
  return { success: true, message: `User ${name} created.` };
}
```

```tsx
// Example usage in a component
// app/page.tsx
import { createUser } from './actions';

export default function HomePage() {
  return (
    <form action={createUser} className="p-4">
      <input type="text" name="name" placeholder="Name" className="border p-2 mr-2" />
      <input type="email" name="email" placeholder="Email" className="border p-2 mr-2" />
      <button type="submit" className="bg-blue-500 text-white p-2 rounded">Create User</button>
    </form>
  );
}
```

**Prompt Example:**

"Generate a server action `createUser` that takes `formData` with `name` and `email` fields and logs them. Also, show an example of how to use it in a form."

### Common Integrations

#### Prisma (schema.prisma)

```prisma
// prisma/schema.prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id        String    @id @default(uuid())
  email     String    @unique
  name      String?
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
}

model Post {
  id        String    @id @default(uuid())
  title     String
  content   String?
  published Boolean   @default(false)
  author    User      @relation(fields: [authorId], references: [id])
  authorId  String
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
}
```

**Prompt Example:**

"Provide a `schema.prisma` example with `User` and `Post` models, configured for PostgreSQL."

**Setup:**

1.  Install Prisma: `npm install prisma --save-dev`
2.  Install Prisma Client: `npm install @prisma/client`
3.  Initialize Prisma: `npx prisma init` (This creates `prisma/schema.prisma` and `.env`)
4.  Update `DATABASE_URL` in `.env`
5.  Migrate database: `npx prisma migrate dev --name init`
6.  Generate Prisma client: `npx prisma generate`

#### Auth.js (NextAuth.js)

```tsx
// app/api/auth/[...nextauth]/route.ts
import NextAuth from "next-auth";
import GitHubProvider from "next-auth/providers/github";
import GoogleProvider from "next-auth/providers/google";

const handler = NextAuth({
  providers: [
    GitHubProvider({
      clientId: process.env.GITHUB_ID ?? "",
      clientSecret: process.env.GITHUB_SECRET ?? "",
    }),
    GoogleProvider({
      clientId: process.env.GOOGLE_CLIENT_ID ?? "",
      clientSecret: process.env.GOOGLE_CLIENT_SECRET ?? "",
    }),
    // Add more providers as needed
  ],
  // Optional: Add database adapter for persistent sessions (e.g., PrismaAdapter)
  // adapter: PrismaAdapter(prisma),
  callbacks: {
    async session({ session, token, user }) {
      // Send properties to the client, like an access_token and user id from a provider.
      // session.accessToken = token.accessToken;
      // session.user.id = user.id;
      return session;
    }
  },
  pages: {
    signIn: '/auth/signin', // Custom sign-in page
  },
});

export { handler as GET, handler as POST };
```

```tsx
// components/auth-button.tsx (Client Component)
'use client';

import { useSession, signIn, signOut } from 'next-auth/react';

export default function AuthButton() {
  const { data: session } = useSession();

  if (session) {
    return (
      <div className="flex items-center space-x-2">
        <p>Signed in as {session.user?.email}</p>
        <button onClick={() => signOut()} className="bg-red-500 text-white px-3 py-1 rounded">Sign out</button>
      </div>
    );
  }
  return (
    <button onClick={() => signIn()} className="bg-green-500 text-white px-3 py-1 rounded">Sign in</button>
  );
}
```

```tsx
// app/providers.tsx (for NextAuth.js session provider)
'use client';

import { SessionProvider } from 'next-auth/react';

export default function Providers({ children }: { children: React.ReactNode }) {
  return (
    <SessionProvider>
      {children}
    </SessionProvider>
  );
}
```

**Prompt Example:**

"Provide `Auth.js` (NextAuth.js) configuration for GitHub and Google providers, along with a client component for sign-in/sign-out buttons and a `SessionProvider` wrapper."

**Setup:**

1.  Install NextAuth.js: `npm install next-auth`
2.  Create `.env.local` with `NEXTAUTH_SECRET` and provider credentials (`GITHUB_ID`, `GITHUB_SECRET`, etc.)
3.  Wrap your `RootLayout` or specific pages with `<Providers>` component.

#### Stripe (Checkout/Payment Intent Snippets)

##### Create Checkout Session (Server Action/API Route)

```tsx
// app/actions/stripe.ts (Server Action)
'use server';

import Stripe from 'stripe';

const stripe = new Stripe(process.env.STRIPE_SECRET_KEY ?? '', {
  apiVersion: '2023-10-16',
});

export async function createCheckoutSession(priceId: string) {
  try {
    const session = await stripe.checkout.sessions.create({
      line_items: [
        {
          price: priceId,
          quantity: 1,
        },
      ],
      mode: 'subscription', // or 'payment' for one-time
      success_url: `${process.env.NEXT_PUBLIC_BASE_URL}/success`,
      cancel_url: `${process.env.NEXT_PUBLIC_BASE_URL}/cancel`,
    });

    return { url: session.url };
  } catch (error) {
    console.error('Error creating checkout session:', error);
    return { error: 'Failed to create checkout session.' };
  }
}
```

##### Redirect to Checkout (Client Component)

```tsx
// components/buy-button.tsx (Client Component)
'use client';

import { createCheckoutSession } from '@/app/actions/stripe'; // Adjust path as needed

interface BuyButtonProps {
  priceId: string;
}

export default function BuyButton({ priceId }: BuyButtonProps) {
  const handleClick = async () => {
    const { url, error } = await createCheckoutSession(priceId);
    if (url) {
      window.location.href = url;
    } else if (error) {
      alert(error);
    }
  };

  return (
    <button
      onClick={handleClick}
      className="bg-purple-600 text-white px-4 py-2 rounded hover:bg-purple-700"
    >
      Buy Now
    </button>
  );
}
```

**Prompt Example:**

"Provide Stripe integration snippets for creating a checkout session using a server action and a client component to redirect the user to the Stripe checkout page."

**Setup:**

1.  Install Stripe: `npm install stripe`
2.  Create `.env.local` with `STRIPE_SECRET_KEY` and `NEXT_PUBLIC_BASE_URL`.
3.  Set up products and pricing in the Stripe Dashboard to get `priceId`.

---

This `SKILL.md` serves as a comprehensive guide for generating Next.js 14+ code with various features and integrations.
